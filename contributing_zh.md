# 参与贡献

语言：[English](contributing.md) · 简体中文 · [한국어](contributing_ko.md)

状态：草案

贡献从插件包清单和公开 SDK 契约开始。提议的包必须说明发布者、许可证、运行时组、路由、能力、资源需求、数据生命周期、支持范围和安全注意事项。

插件包必须可独立构建和测试。它们不得导入 Core 内部模块、与其他包共享运行时状态、暴露宿主端口、包含凭据，或要求未声明的宿主、数据库、网络或浏览器访问。

目录接收不可变的签名版本。包审核检查清单有效性、SDK 兼容范围、签名、校验和、SBOM、许可证、声明能力和风险分类。

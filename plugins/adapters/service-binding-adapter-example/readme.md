# Service Binding Adapter Example

This isolated example forwards only relative paths to an operator-approved Core service binding. It does not contain an upstream URL, credentials, cookies, or browser state.

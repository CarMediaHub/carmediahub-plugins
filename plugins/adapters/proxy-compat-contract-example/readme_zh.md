# 代理兼容契约示例

这是一个不命名或连接真实网站的 isolated-worker 代理兼容层示例。它只使用运营者批准的 Core service binding，只接受相对路径，只转发少量请求头，不转发 Cookie 或凭据，清理重定向和 Cookie 响应头，并拒绝超过 8 MiB 的响应体。

它是契约与安全示例，不是公网中继，也不代表认可任何上游服务。真实适配器在公开前仍需单独完成目标审查、窄范围主机策略、重定向处理、内容重写、法律审查和泄露回归。

# Adapter Plugins

This category is for bounded local-service bridges and upstream protocol adapters. A local-service bridge consumes only an operator-approved Core service binding; it does not modify the upstream project or publish another host port. Each package must declare targets, processing behavior, network permissions, data lifecycle, redirects, response limits, and applicable notices.

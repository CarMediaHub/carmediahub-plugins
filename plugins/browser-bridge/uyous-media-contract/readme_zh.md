# Uyous 媒体契约

这是一个 T2 浏览器辅助媒体工作流的 SDK 契约示例。它只请求 Core 提供的不透明短期浏览器会话和有界任务结果，不访问浏览器 Profile、Cookie、CDP 端点、宿主路径、yt-dlp、FFmpeg 或真实视频平台。

`/extract` 接受逻辑目标标签，并返回 Core 提供的有界结果。正式适配器还必须完成运营者授权、目标 allowlist、撤销、资源限制和上游审查，之后才能考虑进入官方集成。

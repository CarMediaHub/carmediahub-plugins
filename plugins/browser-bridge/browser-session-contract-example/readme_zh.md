# 浏览器会话契约示例

这个隔离示例用于验证 CarMediaHub 的不透明浏览器会话契约。它不会启动浏览器，也不会访问 Profile、Cookie、CDP、宿主路径或任意脚本。

`/session` 路由通过 SDK 请求短期会话元数据。它只用于集成测试，不是浏览器自动化功能，也不是第三方网站适配器。

`/display` 路由展示宿主通过 SDK 传递的语言无关显示能力（设备类型、输入方式、视口和全屏能力），并演示在宿主不支持全屏时返回 `unsupported`。插件只能请求受控模式并读取结果，不能控制浏览器窗口、Profile 或 CDP。

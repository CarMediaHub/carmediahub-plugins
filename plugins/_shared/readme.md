# Shared Plugin Resources

This directory is reserved for SDK-conformance fixtures, manifest validation helpers, and build metadata shared by plugin packages.

Shared resources must not provide cross-plugin runtime state, credentials, host access, database tables, or hidden network communication.
